# Required — PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/smarttimecontrol

# Optional — set to "false" only for local dev with self-signed certs (default: SSL enabled)
# DB_SSL=false
